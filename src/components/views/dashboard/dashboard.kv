# dashboard.kv

#:import utils kivy.utils
<Dashboard>:
    canvas.before:
        Color:
            rgb: app.kynikuiController.theme.background
        Rectangle:
            size: self.size
            pos: self.pos

    BoxLayout:
        orientation: "vertical"
        size_hint: 1.0, 1.0